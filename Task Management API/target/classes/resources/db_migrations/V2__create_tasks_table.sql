CREATE TABLE tasks (
                       id BIGSERIAL PRIMARY KEY,
                       name VARCHAR(255),
                       description TEXT,
                       completed BOOLEAN

);